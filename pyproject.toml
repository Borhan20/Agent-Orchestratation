[project]
name = "test"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "a2a-sdk>=0.2.4",
    "uvicorn>=0.34.2",
]

[tool.uv.workspace]
members = [
    "calender_agent",
    "client",
]
